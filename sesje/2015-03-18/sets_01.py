a = range(0, 1000, 2)
b = range(0, 1000, 3)


def common(first, second):
    """
    Returns sorted list of numbers that are both in `first` and `second` collection.
    """
    # Put your code here.

result = common(a, b)

print(result)